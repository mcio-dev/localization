# Template type
# message,actionbar,title,bossbar
type: message
# Template parameters, same format as commands
arguments:
  --papi: true

# Message content, equivalent to the last part of the command
# Can be a string or a list
messages: 'Example message'
#messages:
#- 'First line'
#- 'Second line'

# Same for title type, here's an example
#messages:
#- 'Title'
#- 'Subtitle'
