# 模板类型
# message,actionbar,title,bossbar
type: message
# 模板参数，与命令的格式相同
arguments:
  --papi: true

# 消息正文，相当于命令最后面的部分
# 可以用字符串，也可以用列表
messages: '示例消息'
#messages:
#- '第一行'
#- '第二行'

# title 类型也是一样的，举个例子
#messages:
#- '标题'
#- '副标题'
